# Community Plugin Preset

This public skeleton commits only plugin IDs and generic Obsidian settings.

The plugin binaries are not vendored. Install plugins from Obsidian Community Plugins, then enable the IDs listed in `.obsidian/community-plugins.json`.

Do not commit downloaded plugin folders, plugin cache files, personal API keys, sync tokens, workspace state, or machine-specific settings into this public framework.
