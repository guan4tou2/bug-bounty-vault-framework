# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-vite-env-json

info:
  name: Vite/SPA env.json runtime config leak (catch-all aware)
  author: hermes-bb-kb
  severity: medium
  description: |
    Real runtime env.json (Vite/SPA) leaks backend/api/client config; excludes SPA 200 shell.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/env.json"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"apiUrl"'
          - '"API_URL"'
          - '"clientId"'
          - '"tenantId"'
          - '"endpoint"'
          - '"backendUrl"'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
          - '<html'
