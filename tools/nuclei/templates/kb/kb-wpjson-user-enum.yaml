# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-wpjson-user-enum

info:
  name: WordPress wp-json user enumeration (real JSON array)
  author: hermes-bb-kb
  severity: low
  description: |
    Confirms exposed author list via real JSON (slug/id), not the default /wp-json discovery.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/wp-json/wp/v2/users"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"slug":'
          - '"id":1,'
          - '"user_registered"'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
          - '<html'
