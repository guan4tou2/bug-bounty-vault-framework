# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-ignition-debug

info:
  name: Laravel Ignition / Whoops debug page (stack trace)
  author: hermes-bb-kb
  severity: high
  description: |
    Ignition health-check JSON; also catches Whoops via stack-trace words on error pages.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/_ignition/health-check"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"can_execute_commands"'
          - 'ignition'
          - '"environment"'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
