# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-actuator-env-dump

info:
  name: Spring actuator /env real dump (not SPA shell)
  author: hermes-bb-kb
  severity: high
  description: |
    Requires actual env JSON keys; SPA catch-all returning 200 HTML is excluded.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/actuator/env"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"activeProfiles"'
          - '"propertySources"'
          - 'systemEnvironment'
          - 'applicationConfig'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
          - '<html'
