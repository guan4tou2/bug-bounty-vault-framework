# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-source-map-exposed

info:
  name: JS source map with sourcesContent exposed
  author: hermes-bb-kb
  severity: low
  description: |
    Real source map reveals original source; helps find internal pkg names / DepConf scopes.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/main.js.map"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"sourcesContent"'
          - '"webpack://"'
          - '"sources":['
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
