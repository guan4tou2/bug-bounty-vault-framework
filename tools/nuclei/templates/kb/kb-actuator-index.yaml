# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-actuator-index

info:
  name: Spring actuator index exposed (endpoint list, catch-all aware)
  author: hermes-bb-kb
  severity: medium
  description: |
    Actuator index JSON lists exposed mgmt endpoints; SPA 200 shell excluded. KB: Unauth Security Dashboard Chain.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/actuator"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '"_links"'
          - '"health":{"href"'
          - '"/actuator/env"'
          - '"/actuator/health"'
          - '"beans"'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
          - '<html'
