# AUTO-GENERATED by scripts/bb_kb_to_nuclei.py from team KB. GET-only, catch-all-aware.
id: kb-prometheus-metrics

info:
  name: Prometheus /metrics exposed (real metric format)
  author: hermes-bb-kb
  severity: low
  description: |
    Real Prometheus exposition format (# HELP/# TYPE); excludes SPA shell. KB: Unauth Security Dashboard Chain.
  tags: kb,exposure,catch-all-aware,safe

http:
  - method: GET
    path:
      - "{{BaseURL}}/metrics"
    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: word
        part: body
        condition: or
        words:
          - '# HELP '
          - '# TYPE '
          - 'process_cpu_seconds_total'
          - 'go_gc_duration_seconds'
          - 'jvm_'
      - type: word
        part: body
        condition: or
        negative: true
        words:
          - '<!doctype html'
          - '<html'
